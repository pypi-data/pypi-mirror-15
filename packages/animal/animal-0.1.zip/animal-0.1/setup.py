#!/usr/bin/env python3
from distutils.core import setup
setup(
  name = 'animal',
  packages = ['animal'], # this must be the same as the name above
  version = '0.1',
  description = 'A random animal lib',
  author = 'Carlos Clavero',
  keywords = ['testing', 'logging', 'example'], # arbitrary keywords
  classifiers = [],
)

