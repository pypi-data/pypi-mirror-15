rmin = 1.4
rmax = 3.0
offset = 0

dset = out.datasets[0]

irmin = rmin/(dset.transform.rstep)
irmax = rmax/(dset.transform.rstep)

resid = realimag(dset.data.chir[irmin:irmax] - dset.model.chir[irmin:irmax])/ dset.epsilon_r

chi2  = dset.n_idp * (resid**2).sum()/len(resid)
