# http://docs.python.org/2/distutils/sourcedist.html#the-manifest-in-template

# python setup.py dist arguments:
# --no-defaults 	(disable the standard “include”)
# --no-prune 		(disable the standard “exclude”)
# --manifest-only 	(generate the MANIFEST only)

# setup(**kwargs) "py_modules", scripts included by default

# exclude all bytecode (excluded by default)
# global-exclude *.py[co]

global-exclude *.command 
# exclude .command (OS X)

exclude __pycache__/* LICENSE* README* description

include data_files/* Examples/* Tests/* *.txt
