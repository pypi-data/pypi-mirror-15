.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install tenv

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv tenv
    $ pip install tenv
